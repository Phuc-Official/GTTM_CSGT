class Vehicle {
  final String logId; // ID của log
  final String type; // Loại xe
  final String plateNo; // Biển số
  final String iconPath; // Đường dẫn hình ảnh

  Vehicle({
    required this.logId,
    required this.type,
    required this.plateNo,
    required this.iconPath,
  });
}